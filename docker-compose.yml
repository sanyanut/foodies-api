# Local development stack for the Foodies API:
#   - postgres : PostgreSQL 18 database
#   - app      : the Express API (independent container)
#
# In production these run as SEPARATE managed services (Render Postgres +
# Render Web Service); this compose file only exists for local development.
services:
  postgres:
    image: postgres:18
    container_name: foodies-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${DB_HOST_PORT}:5432"
    volumes:
      # PostgreSQL 18+ Docker images store the cluster in a version-specific
      # subdirectory, so the volume is mounted at /var/lib/postgresql (NOT
      # /var/lib/postgresql/data, which was the convention up to PG 17).
      - foodies_pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: foodies-api
    ports:
      - "${APP_PORT}:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      # Inside the compose network the DB host is the service name "postgres",
      # not "localhost". This overrides DATABASE_URL from .env (dotenv does not
      # overwrite variables already present in the process environment).
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - POPULAR_RECIPES_CACHE_TTL=${POPULAR_RECIPES_CACHE_TTL}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    # Regenerate the Prisma client (the bind mount hides the image's copy),
    # apply migrations against the container database, then start the API in
    # watch mode. The source bind mount + CHOKIDAR_USEPOLLING give hot reload,
    # so "local development entirely in Docker" restarts on every code change.
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run dev"

volumes:
  foodies_pgdata:
